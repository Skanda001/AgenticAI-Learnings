# Core LangChain (LCEL / Runnables, chains, agents)
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-mistralai

# OpenAI SDK (used under the hood by langchain-openai, kept explicit for direct calls if needed)
openai>=1.40.0

# Tavily search tool
tavily-python>=0.5.0
langchain-tavily>=0.1.0

# BeautifulSoup tool (web scraping)
beautifulsoup4>=4.12.0
lxml>=5.2.0
requests>=2.32.0

# Env variable management (API keys)
python-dotenv>=1.0.0

# Pydantic (schema validation for tools/agents, LangChain v0.3 uses pydantic v2)
pydantic>=2.7.0

# Optional but commonly needed for React-style agent tracing/debugging
langsmith>=0.1.100

streamlit